export enum DEPLOY_MODE {
  PREVIEW = 'preview',
  UPLOAD = 'upload',
}

export enum RUNTIME {
  CI = 'CI',
  NONE = '',
  CLI = 'CLI',
}

export enum HISTORY_TYPE {
  BROWSER = 'BROWSER',
  HASH = 'HASH',
}
